(["*scratch*" ";; This buffer is for text that is not saved, and for Lisp evaluation.
;; To create a file, visit it with <open> and enter text in its buffer.

(setq a 10)
" (1 . 1) lisp-interaction-mode nil nil])